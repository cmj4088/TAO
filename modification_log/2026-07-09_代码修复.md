# 2026-07-09 代码修复：版本号统一+端口统一+文档同步+开机连接检查

## 新增：开机后端连接检查

### ConnectionBanner 组件
- 新建 `frontend/src/components/ConnectionBanner.tsx`
- 应用启动时自动 ping 后端 `/api/version`（5 秒超时）
- 连接失败时在页面顶部显示红色警告横幅："未连接到服务器（http://localhost:8002）"
- 每 30 秒自动轮询重试，连接恢复后横幅自动消失
- 用户可手动点击"重试"按钮立即检测
- 在 `App.tsx` 中引入，与 `UpdateNotification` 同级

### 用户需求
> "加入一个开机检查是否和后端连接的小界面，如果没有就在UI显示没有连接到服务器"

---
# 2026-07-09 代码修复：版本号统一+端口统一+文档同步

## 修复的问题

### 1. 版本号统一为 0.1.3
- `backend/app/main.py`：`VERSION = "0.1.0"` → `"0.1.3"`
- `frontend/src/components/VersionBadge.tsx`：硬编码 `"0.1.0"` → `"0.1.3"`
- `frontend/electron-main/preload.cjs`：已是 `"0.1.3"`（无需修改）
- `frontend/package.json`：已是 `"0.1.3"`（无需修改）

### 2. API地址统一为 localhost:8002
- `frontend/src/api/client.ts`：`API_BASE` 从 `http://10.50.150.176:8006` → `http://localhost:8002`
- `frontend/src/stores/settingsStore.ts`：`apiBase` 从 `http://10.50.150.176:8006` → `http://localhost:8002`
- 与 `docker-compose.yml` 端口映射 `8002:8002` 一致
- 与 `Dockerfile` EXPOSE 8002 一致

### 3. docs/agent-guide.md 全面更新
- 更新版本号：0.1.0 → 0.1.3
- 更新端口：8000 → 8002
- 补充完整 API 端点（20+个，非原来的8个）
- 补充 settingsStore 完整字段（llmModel、hasKey、app02AiConcurrency）
- 补充数据库预置配置（llm_model、app02_ai_concurrency）
- 补充加密机制说明（AES-256-CBC）
- 补充 AI 审查 SSE 流式说明
- 补充 Docker 部署规范（ENCRYPTION_KEY）
- 补充 Electron 更新机制（electron-updater）
- 补充所有 App 始终挂载的设计决策
- 更新已知陷阱（新增3条）

## 影响文件
- `backend/app/main.py`
- `frontend/src/components/VersionBadge.tsx`
- `frontend/src/api/client.ts`
- `frontend/src/stores/settingsStore.ts`
- `docs/agent-guide.md`