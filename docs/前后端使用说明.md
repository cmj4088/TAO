第一步：启动后端

打开一个终端，输入：


cd c:/Users/32277/Desktop/TAO/backend
pip install -r requirements.txt   # 首次需要安装依赖
uvicorn app.main:app --port 8000
看到 Uvicorn running on http://127.0.0.1:8000 就说明后端跑起来了。

第二步：启动前端

再开一个新终端，输入：


cd c:/Users/32277/Desktop/TAO/frontend
npm install    # 首次需要安装依赖
npm run dev